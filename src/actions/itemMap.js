// Item Map Actions
export const setBaseType = (baseType) => ({
    type: 'setBaseType',
    baseType
});

export const setItemImg = (itemImg) => ({
    type: 'setItemImg',
    itemImg
});

export const setItemSlot = (itemSlot) => ({
    type: 'setItemSlot',
    itemSlot
});

export const setItemBaseName = (itemBaseName) => ({
    type: 'setItemBaseName',
    itemBaseName
});